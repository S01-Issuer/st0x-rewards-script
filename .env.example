# HyperSync API Configuration
# Obtain your API key from https://hypersync.xyz/
HYPERSYNC_API_KEY=cd0ca998-96a2-4372-8ecf-bd338b662481

# Optional: RPC endpoint if needed for direct chain queries
# RPC_URL=https://mainnet.base.org

# Optional: Custom output directory for CSV files
# OUTPUT_DIR=./output

# Optional: Logging level (debug, info, warn, error)
# LOG_LEVEL=info

# Optional: Use cached data instead of fetching from API
# Set to 'true' to load previously cached event data from ./cache directory
# USE_CACHE=true
